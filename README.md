# lol-team-analitycs
Find summoner's champs info

## Getting Started  
* Download the repo
* run "npm install op.gg-api"
* run "git reset --hard" 
* run "npm install console.table --save"

The magic its done